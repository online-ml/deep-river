name: unit-tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        python: ['3.10', '3.11', '3.12', '3.13']
        os: [ubuntu-latest, macos-latest, windows-latest]
        dependency-set: ['minimum', 'maximum']

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python ${{ matrix.python }}
        run: uv python install ${{ matrix.python }}

      - name: Cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-${{ matrix.python }}-${{ matrix.dependency-set }}-${{ hashFiles('**/uv.lock') }}
          restore-keys: |
            uv-${{ runner.os }}-${{ matrix.python }}-${{ matrix.dependency-set }}-
            uv-${{ runner.os }}-${{ matrix.python }}-
            uv-${{ runner.os }}-

      - name: Install dependencies (minimum versions)
        if: matrix.dependency-set == 'minimum'
        run: |
          uv sync --resolution=lowest-direct --all-extras

      - name: Install dependencies (maximum versions)
        if: matrix.dependency-set == 'maximum'
        run: |
          uv sync --resolution=highest --all-extras

      - name: Download datasets
        run: uv run python -c "from river import datasets; datasets.Bikes().download(); datasets.CreditCard().download(); datasets.Elec2().download(); datasets.Keystroke().download()"

      - name: Run tests (generate coverage.xml)
        run: |
          uv run pytest -v --cov=deep_river --cov-report=term-missing --cov-report=xml:coverage.xml -m "not datasets"
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Upload coverage artifact (diagnostic)
        uses: actions/upload-artifact@v4
        if: always()
        with:
            name: coverage-${{ matrix.os }}-py${{ matrix.python }}-${{ matrix.dependency-set }}
            path: coverage.xml
            if-no-files-found: warn

      - name: Upload coverage report to Codecov (aggregate project)
        if: github.repository == 'online-ml/deep-river' && matrix.os == 'ubuntu-latest' && matrix.python == '3.13' && matrix.dependency-set == 'maximum'
        uses: codecov/codecov-action@v4
        with:
          use_oidc: true
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          name: ${{ matrix.os }}-py${{ matrix.python }}-${{ matrix.dependency-set }}
          fail_ci_if_error: true
          verbose: true
          disable_search: true
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
